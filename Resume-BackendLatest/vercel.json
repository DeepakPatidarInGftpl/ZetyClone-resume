{
  "version": 2,
  "builds": [
    { "src": "app.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/app.js" }
  ],
  "scripts": {
        "build": "npm install --force && npm run build"
    }
}
